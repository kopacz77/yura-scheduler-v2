// Shared types used across multiple files
export type Theme = 'light' | 'dark' | 'system';

export type ColorScheme = {
  primary: string;
  secondary: string;
  accent: string;
  background: string;
  text: string;
  error: string;
  success: string;
  warning: string;
};
